import React from "react";
import { poppins } from "./utils/fonts";

const Home = () => {
  return (
    <div>
      <h1 className={`${poppins.className} antialiased`}>Welcome to the Home Page</h1>
    </div>
  );
};

export default Home;
